spring:
  application:
    name: blog-cms
  datasource:
    url: jdbc:mysql://localhost:3306/blog_cms?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 2005.Zzy@05060o?
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  output:
    ansi:
      enabled: always

jwt:
  secret: your-secret-key-must-be-at-least-256-bits-long-for-hs256-algorithm
  expiration: 86400000

file:
  upload-dir: ./uploads

server:
  port: 8080

logging:
  level:
    com.cms: DEBUG